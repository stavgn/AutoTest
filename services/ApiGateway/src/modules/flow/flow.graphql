type Flow {
    _id: ID!
    user: User!
    baseUrl: String!
    sequences: [Sequence]
}

extend type Query {
    getFlowById(id: ID!): Flow
}

extend type Mutation {
    createFlow(baseUrl: String!): Flow
}